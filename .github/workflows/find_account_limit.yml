name: Find Account Limit

on: workflow_dispatch

jobs:
  analyze:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Analyze
        run: |
          echo "MacOS runner ready"
          uname -a
          which nm otool python3
